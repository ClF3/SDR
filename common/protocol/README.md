# Protocol

Shared protocol definitions belong here.

This directory should become the source of truth for:

- IQ packet header layout
- PSD packet layout
- status schema
- control command schema
- protocol versioning

